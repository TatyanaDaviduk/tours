const swiper = new Swiper('.swiper', {
    // Optional parameters
    autoHeight: true,
    slidesPerView: 1,
    loop:true,


    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  

  });